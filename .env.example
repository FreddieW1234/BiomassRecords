# Baked in at build time (Render env vars override these there).
# Connection Lab can override both at runtime without a rebuild.
VITE_API_URL=https://api.biomasswood.co.uk
VITE_API_KEY=paste-the-API_KEY-from-the-office-server
